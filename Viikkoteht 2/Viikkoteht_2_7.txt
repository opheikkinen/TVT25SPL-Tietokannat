A)

SELECT * FROM employees ORDER BY last_name LIMIT 10;

B)

SELECT * FROM employees ORDER BY last_name, first_name  LIMIT 10;

C)

SELECT * FROM employees ORDER BY hire_date DESC  LIMIT 10;

D)

SELECT first_name, last_name, salary FROM employees JOIN salaries ON employees.emp_no=salaries.emp_no ORDER BY salary DESC LIMIT 1;

E)

SELECT first_name, last_name, salary FROM employees JOIN salaries ON employees.emp_no=salaries.emp_no ORDER BY salary LIMIT 1;

F)

SELECT * FROM employees JOIN salaries ON employees.emp_no=salaries.emp_no WHERE salary>150000;

G)

SELECT COUNT(*) FROM dept_emp WHERE dept_no = 'd007';

SELECT COUNT(*) FROM dept_emp WHERE dept_no = 'd001';

H)

SELECT first_name, last_name, dept_no FROM employees JOIN dept_manager ON employees.emp_no=dept_manager.emp_no;

I)

SELECT AVG(salary) FROM employees 
JOIN salaries ON employees.emp_no=salaries.emp_no 
JOIN dept_emp ON employees.emp_no=dept_emp.emp_no 
WHERE dept_no='d007';

SELECT AVG(salary) FROM employees 
JOIN salaries ON employees.emp_no=salaries.emp_no 
JOIN dept_emp ON employees.emp_no=dept_emp.emp_no 
WHERE dept_no='d001';